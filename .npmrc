# 本地开发时使用镜像源加速 pnpm install
# 注意：发布时 changeset publish 会读取 libs/vite-plugin-crx-hmr/package.json
#       中的 publishConfig.registry（指向 https://registry.npmjs.org/），
#       与此处的镜像源互不干扰，不会影响发布目标
registry=https://registry.npmmirror.com/